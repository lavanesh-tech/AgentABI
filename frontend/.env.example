# Base URL of the AgentABI backend's v1 API. Browser-visible (NEXT_PUBLIC_*)
# because the frontend calls it directly from client components — never put
# a secret in a NEXT_PUBLIC_* variable.
NEXT_PUBLIC_AGENTABI_API_URL=http://localhost:8000/api/v1
